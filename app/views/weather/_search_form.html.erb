<%= turbo_frame_tag "search_form" do %>
  <%= form_with url: search_weather_path, method: :get, data: { turbo_stream: true } do |form| %>
    <div class="space-y-4">
      <div>
        <%= form.label :state, "State", class: "block text-sm font-medium text-gray-700" %>
        <%= form.select :state,
            options_for_select(states_for_country, params[:state]),
            { include_blank: "Select a state" },
            class: "bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5",
            required: true,
            data: {
              action: "change->location#updateOptions",
              location_target: "stateSelect"
            } %>
      </div>

      <div>
        <%= form.label :city, "City", class: "block text-sm font-medium text-gray-700" %>
        <%= form.select :city,
            options_for_select(cities_for_state(params[:state]), params[:city]),
            { include_blank: "Select a city" },
            class: "bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5",
            required: true,
            data: { location_target: "citySelect" } %>
      </div>

      <%= form.submit "Get Weather",
          class: "w-full bg-blue-600 text-white py-2 px-4 rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2" %>
    </div>
  <% end %>
<% end %>
